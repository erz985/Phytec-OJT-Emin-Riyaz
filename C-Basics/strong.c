#include<stdio.h>
#include<math.h>
int main()
{
        int n,rem,sum=0,count=0,temp,temp1;
        printf("Enter a number to check if it's an Strong Number : ");
        scanf("%d",&n);
       
        
        if(sum==temp1)
                printf("It is an Armstrong Number\n");
        else
                printf("Not an Armstrong Number\n");
        
        return 0;        
}
